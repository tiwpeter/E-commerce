# ── API ──────────────────────────────────────────────────────────
# Backend API URL (used in browser with NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# OpenAPI spec URL for orval generate (dev only)
ORVAL_API_SPEC_URL=http://localhost:5000/api-docs.json
