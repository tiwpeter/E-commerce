# ── API ──────────────────────────────────────────────────────────
# URL ของ Backend API (ใช้ใน browser ด้วย prefix NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL=http://localhost:5000

# URL ของ OpenAPI spec สำหรับ orval generate (ใช้ตอน dev เท่านั้น)
ORVAL_API_SPEC_URL=http://localhost:5000/api-docs.json
